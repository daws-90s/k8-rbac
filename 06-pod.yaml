apiVersion: v1
kind: Pod
metadata:
  name: aws-cli # pod name
  namespace: roboshop
  labels:
    project: roboshop
    environment: dev
    purpose: poc
spec:
  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command: ["sleep", "1000"]

    